# Seed recommended CSVs
#
# Files here named `alphasignal-recommended-YYYY-MM-DD.csv` are imported into
# history on boot **only if that calendar day is missing** (e.g. after an
# ephemeral Render disk wipe). Safe to leave in place — days already present
# are skipped.
