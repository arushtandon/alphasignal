# Install Bloomberg's Python wheel FIRST (see bloomberg-bridge README):
#   pip install --index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi
#
# NumPy: use wheels only (no MSVC). NumPy 2.x is OK — bridge.py restores np.NaN for pdblp.
# If pip tries to compile NumPy, upgrade pip and use Python 3.10–3.12 x64, or:
#   pip install numpy pandas --only-binary=:all:
flask>=3.0
pdblp>=0.1.3
pandas>=2.0,<3
