# Install Bloomberg's Python wheel FIRST (see bloomberg-bridge README):
#   pip install --index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi
#
# Windows without Visual Studio: do NOT compile NumPy from source. Either:
#   .\install_deps.ps1
# or PowerShell/cmd:
#   python -m pip install --upgrade pip setuptools wheel
#   python -m pip install numpy "pandas>=2.0,<3" --only-binary=:all:
#   python -m pip install -r requirements.txt
#
# NumPy 2.x is fine — bridge.py patches np.NaN / np.NAN before and after pdblp import (pdblp uses np.NaN).
flask>=3.0
pdblp>=0.1.3
pandas>=2.0,<3
