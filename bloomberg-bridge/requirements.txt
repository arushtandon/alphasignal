# Install Bloomberg's Python wheel FIRST (automated by run-bridge.bat / run-bridge.ps1):
#   pip install --index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/ blpapi
flask>=3.0
pdblp>=0.1.3
# pdblp 0.1.x is not NumPy-2-clean; keep 1.x unless you patch venv
numpy>=1.26,<2
pandas>=2.0,<3
